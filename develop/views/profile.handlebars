<!DOCTYPE html>
<html>
<head>
    <title>User Profile</title>
</head>
<body>
    <h1>User Profile</h1>
    <div class="profile">
        <h2>{{user.name}}</h2>
        <p>Email: {{user.email}}</p>
        <p>Location: {{user.location}}</p>
        <p>Bio: {{user.bio}}</p>
    </div>
</body>
</html>